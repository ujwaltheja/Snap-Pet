package com.snappet.ui.theme

import androidx.compose.ui.graphics.Color

// Vibrant Game Palette
val GamePrimary = Color(0xFFFF9F1C) // Vibrant Orange
val GameOnPrimary = Color.White
val GamePrimaryContainer = Color(0xFFFFE5B4)
val GameSecondary = Color(0xFF2EC4B6) // Teal
val GameBackground = Color(0xFFCBF3F0) // Light Mint
val GameSurface = Color.White
val GameOnSurface = Color(0xFF2B2D42) // Dark Blue-Grey

// Stat Colors (Vibrant)
val StatHunger = Color(0xFFFF6B6B) // Coral Red
val StatHappiness = Color(0xFFFFD93D) // Sunny Yellow
val StatEnergy = Color(0xFF6BCF7F) // Emerald Green
val StatHygiene = Color(0xFF4DD0E1) // Cyan

// UI Accents
val AccentPurple = Color(0xFF9D4EDD)
val AccentPink = Color(0xFFFF006E)
val AccentBlue = Color(0xFF3A86FF)

// Gradients
val BgGradientStart = Color(0xFFCBF3F0)
val BgGradientEnd = Color(0xFFFFFFFF)

val GlassWhite = Color.White.copy(alpha = 0.25f)
val GlassBorder = Color.White.copy(alpha = 0.5f)
